info:
  name: Cancel
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/transmissions/{{transmissionToken}}/cancel
  headers:
    - name: authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
