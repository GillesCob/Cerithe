// Page volontairement vide pour l'instant : la liste des biens est affichée directement dans le
// Dashboard. Le jour où le Dashboard élargira son contenu (transmissions en cours, chantiers en
// cours, etc. — pas encore figé), la liste des biens actuellement dans DashboardPage migrera ici.
const PropertyListPage = () => {
  return <div>On est dans PropertyListPage</div>;
};

export default PropertyListPage;
